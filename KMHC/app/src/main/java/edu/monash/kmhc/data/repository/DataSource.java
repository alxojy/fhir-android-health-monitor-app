package edu.monash.kmhc.data.repository;

import org.hl7.fhir.r4.model.Patient;

import java.util.ArrayList;

/**
 * Interface is responsible in providing abstraction so that there's low coupling between the caller
 * class and the concrete implementation.
 */
public interface DataSource {

}
